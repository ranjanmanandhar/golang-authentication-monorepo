package repository

import (
	"testing"
)

var redisUrl = "localhost:6379"
var username = "prabeshl_home"

func TestVerifyClient(t *testing.T) {
	// redisService := service.NewRedisClient(redisUrl)
	// authRepo := New(*redisService)

	// authRepo.VerifyClient(username)
}
